name: Regenerate shared layout once

on:
  push:
    branches:
      - main
    paths:
      - "partials/sidebar.html"
      - ".github/workflows/regenerate-layout-once.yml"

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Regenerate shared layout
        run: node scripts/update-layout.cjs

      - name: Verify generated layout
        run: node scripts/update-layout.cjs --check

      - name: Commit generated HTML
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- index.html docs blog; then
            echo "No generated layout changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html docs blog
          git commit -m "Regenerate sidebar layout"
          git push
